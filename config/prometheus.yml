global:
  scrape_interval: 10s

scrape_configs:
  - job_name: 'eco-bottle-api'
    static_configs:
      - targets: ['api:4000']
        labels:
          service: 'api'
          metrics_path: '/metrics'
